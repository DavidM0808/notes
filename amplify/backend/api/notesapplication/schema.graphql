type Notes @model @auth(rules: [{allow: owner}]) {
  id: ID!
  title: String
  text: String
}

type Mutation {
  customCreateItem(input: CreateItemInput!): Item
    @function(name: "notesCurdFunction-${env}")
}
 