type Notes @model @auth(rules: [{allow: owner}]) {
  id: ID!
  title: String
  text: String
}

input CreateItemInput {
  id: ID!
  title: String
  text: String
}

type Mutation {
  customCreateNotes(input: CreateItemInput!): Notes
    @function(name: "notesCurdFunction-${env}")
}
 