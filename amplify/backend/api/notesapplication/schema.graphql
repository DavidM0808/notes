type Notes @model @auth(rules: [{allow: owner}]) {
  id: ID!
  title: String
  text: String
}

input CreateNotesInput {
  id: ID!
  title: String
  text: String
}

type Mutation {
  customCreateNotes(input: CreateNotesInput!): Notes
    @function(name: "notesCurdFunction-${env}")
}
 