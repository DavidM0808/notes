type Notes @model 
@auth(
  rules: [
      {
      allow: owner,
      ownerField: "owner",
      operations: [create, delete, update, read] 
      }
    ]
  ) {
  id: ID!
  title: String
  text: String
  owner: String
}
 